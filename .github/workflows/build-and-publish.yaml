name: Build and Publish

on:
  push:
    branches: [ "master" ]
    tags:
      - '*'
  workflow_dispatch:

jobs:
  run:
    uses: ticketsbot-cloud/template/.github/workflows/template.yaml@main
    with:
      package_name: autoclosedaemon
      sha: ${{ github.sha }}
    secrets: inherit
